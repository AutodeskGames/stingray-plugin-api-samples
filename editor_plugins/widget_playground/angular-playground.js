define(['app'], function (app) {
    'use strict';

    // Angular controller
    app.controller('widgetPlaygroundController', function ($scope) {
        $scope.placeholder = "Put your HTML5 elements here.";
    });
});
